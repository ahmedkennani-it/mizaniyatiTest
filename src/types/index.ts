// Types partagés des entités du domaine.
// Point d'entrée conventionnel vers les types définis dans la couche de
// persistance locale (aucune donnée réseau — voir `src/db`).
export type * from '../db/repositories/types';
